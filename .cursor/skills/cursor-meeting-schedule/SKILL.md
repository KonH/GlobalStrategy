---
name: cursor-meeting-schedule
description: "(CURSOR) Create a new multi-agent meeting under Docs/Meetings/ for Claude/Codex/Cursor agents. Use when asked to schedule/set up a meeting. Cursor-specific wrapper — prefer this over the Claude meeting-schedule skill."
---

# (CURSOR) Meeting schedule

Follow `.claude/skills/meeting-schedule/SKILL.md` exactly — it's the
canonical, tool-agnostic procedure (directory layout, `agenda.md` template,
copying `scripts/meetings/log_template.html` to `log.html`). Nothing in it
is Claude-specific.

If both a Claude `meeting-schedule` skill and this skill are listed, use
this one.
