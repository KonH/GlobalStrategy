# Eval Summary: control

Supersedes `discoverAndControl` (discovery removed; control-only feature with no tunable parameters).

Attempts: 0
Latest attempt: none

Eval history was reset for a clean `control` baseline — prior `discoverAndControl` results are obsolete.
