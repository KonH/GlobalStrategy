# Eval Summary: warStack

Attempts: 0
Latest attempt: none

`warStack` is not a registered `featureId`. Invoke the shared stack config via:

```
dotnet run --project src/Game.Evals -- --feature warProsecute --eval-config Docs/BotFeatures/warStack/eval_config.json
```

Command-on asserts `warProsecute` / `sell_arms`. Unlock and declare emission bars remain proven by the per-feature package runs. Long-horizon control-only twin; live batch runs deferred.
