# Spec: Card UI & Discard Rework

## Feature Intent

As a player managing my hand of action cards, I want to see every requirement gating a card, which countries could play it, and a way to discard an unwanted card for gold, so that I can make informed decisions about my hand instead of guessing at hidden gating logic or being stuck holding a dead card.

## Acceptance Criteria

Legend: `Precondition => Action => Outcome`, grouped under a shared precondition where one applies to several rows.

### All requirements shown (issue item 1)

- The player opens a country's or the org's Actions panel and looks at any card currently in hand (playable or unplayable).
  - The player looks at the card => every requirement gating that card is listed, not just the first failing one; each line clearly shows pass/fail (e.g. color/icon) and is phrased in plain player language, not a raw technical expression (e.g. "Needs 30+ opinion", not `opinion (35) >= 30`).
  - The card has a gold cost => cost affordability appears as one more line in that same requirements list, not only as the separate cost/gold footer that already exists.
- The player looks at the same card face rendered as the deck-pile stack (face-down) or as the flying draw/play animation copy or the fixed test-card overlay slot used mid-play.
  - The card is face-down (deck pile) => no requirements text is expected there (nothing is legible on a card back); the pile's card silhouette still matches the new taller size (see below) so it doesn't visually mismatch the hand.
  - The card is face-up (flying/animated or test-card copy) => it shows the same requirements list content as the equivalent hand card would, consistent with the sibling war-win-chance badge already staying consistent across these same surfaces.
- Any rendering of the card shape — hand card, deck-pile back, or flying/test-card animation copy.
  - The card's height increases by 20% relative to today, while its width stays exactly the same => all three renderings keep the same taller-but-same-width silhouette in sync (no mismatch between the deck stack and hand cards, no visible jump in size mid-animation).
  - The existing header, art image, and description keep their current visual size and sit at the top of the now-taller card => the entire extra height is used by the new requirements block at the bottom, not spread proportionally across every element.

### Playable-countries badge (issue item 3)

- The player views a country-targeted hand card that more than one country could play.
  - The player looks at the card art => a badge appears on the left side of the card art, mirroring the war-win-chance badge's position on the right, showing flags for up to 2 of the countries that could play this card (any 2 — order is not significant).
  - More than 2 countries could play the card => only the first 2 flags are shown, followed by literal `"..."` to their right (not a `"+N"` count).
  - The player hovers the badge => a tooltip appears listing every country that could play the card, not just the visible 2.
- The player views a country-targeted hand card that only one country could play.
  - The player looks at the card art => the badge shows that single country's flag with no `"..."` suffix.
- The player views a card that no country context makes sense for (see Ambiguities for org cards).
  - No badge is required to appear.

### Discard (issue item 5)

- The player has a card in hand (not currently mid card-play animation) and presses down on it without releasing.
  - The player releases within 1 second while still over the card => the card plays exactly as it does today; no discard hint ever appeared, existing play behavior is unchanged.
  - The player keeps the pointer pressed for 1 second or more => a hint appears on/near the card reading "Discard for 50 <gold-icon>" (the number reflects the current configurable discard cost, not a hardcoded 50).
- The discard hint is showing and the player currently has enough gold to cover the discard cost.
  - The player releases while still over the card => the card is discarded from hand and the gold cost is deducted; the hand is refilled per the sibling deck spec's draw mechanics (this spec does not define the draw-side mechanics of the replacement).
  - The player releases after the pointer has moved off the card => the hint disappears; nothing else happens — no discard, no gold spent, the card stays in hand.
- The discard hint is showing and the player does NOT have enough gold to cover the discard cost.
  - The hint is showing => its price text renders in the same red used elsewhere for unaffordable costs, giving the player a clear signal before releasing.
  - The player releases while still over the card anyway => no discard happens and no gold is deducted; a floating "not enough gold" notification appears near the card; the card remains in hand.

## Tech Notes

### All requirements — layout & data

- **Height/width change (single CSS change, but touches every visual copy independently):**
  - `.action-card` in `Assets/UI/Overlay/OrgInfo/OrgActions.uss` (~line 32): `height: 300px` → `height: 360px` (20% increase), `width: 240px` stays unchanged. `.action-card-header { height: 32px }` and `.action-card-art { height: 130px }` (same file) are left as-is per the issue's "all content moves up except requirements block" — the +60px is allocated to the new requirements block, not spread across header/art/desc.
  - `CardTransitionView.PlaceAndAnimate` (`Assets/Scripts/Unity/UI/CardTransitionView.cs`, lines ~76-77) hardcodes `_cardCopy.style.width = 240f; _cardCopy.style.height = 300f;` independently of the USS class — this literal pair **must** be updated to `240f` / `360f` in lockstep with the USS change, or the flying-card copy renders at the old height while the resting hand card is taller. This is the single highest-risk spot for the two sizes drifting apart.
  - Deck pile (`CountryActionsView.BuildDeckPile`, `OrgActionsView.BuildDeckPile`) applies the raw `.action-card` class to its shadow/front elements with no `ActionCardBuilder` call — picks up the new height automatically from the USS change; no code change needed there beyond the USS edit itself.
- **Card face builder — where the requirements list is actually built:**
  - `Assets/Scripts/Unity/UI/ActionCardBuilder.cs`, `Populate()` (shared by `Build()` and `PopulateSlot()`) is the single place that lays out header → art (+ optional war-win-chance badge, + new playable-countries badge) → body (desc + footer/cost). Add a new requirements-list section, e.g. built from a new `IReadOnlyList<(string Text, bool Passed)>` parameter, appended after the existing footer inside `action-card-body` (or as a new sibling section after body inside `action-card-content` — exact placement flagged in Ambiguities). New USS classes, e.g. `.action-card-requirements`, `.action-card-requirement-row`, `.action-card-requirement-row--pass` / `--fail`, colored via the existing `gs-color-positive` / `gs-color-negative` shared classes rather than inventing new colors.
  - `ActionCardBuilder` is called from exactly **three** live call sites for country cards, all of which need the new parameter threaded through:
    1. `CountryActionsView.BuildHandCard()` (`Assets/Scripts/Unity/UI/CountryActionsView.cs`, ~line 65) — the primary hand card.
    2. `CardTransitionView.Show()` / `ShowCountry()` (`Assets/Scripts/Unity/UI/CardTransitionView.cs`) — the flying draw/play animation copy.
    3. `CardPlayAnimator.PopulateCountryTestCard()` / `PopulateTestCard()` (`Assets/Scripts/Unity/UI/CardPlayAnimator.cs`, ~399-421) — the fixed `card-test-card` overlay slot used mid-play, via `ActionCardBuilder.PopulateSlot`.
  - **`OrgActionsView.BuildHandCard()` does NOT call `ActionCardBuilder` at all** — confirmed by `grep`: only `CardTransitionView.cs`, `CardPlayAnimator.cs`, `CharactersView.cs`, and `CountryActionsView.cs` reference `ActionCardBuilder`. `OrgActionsView` independently duplicates the header/art/body/footer markup inline using the same `.action-card*` class names. This means: (a) the height/width USS change applies to org cards automatically (same CSS classes), but (b) the requirements-list markup must be added to `OrgActionsView.BuildHandCard` by hand — it will not appear there "for free" just because `ActionCardBuilder` changed. Confirm scope in Ambiguities before implementing both paths.
- **Data already available, just unused by player UI:**
  - `ActionCardEntry.Conditions` (`src/Game.Main/VisualState.cs:271`, `IReadOnlyList<ActionConditionDebugEntry>`) is already populated by `VisualStateConverter.BuildEntry` (`src/Game.Main/VisualStateConverter.cs`, ~641-699) via `ActionConditionDebug.EvaluateAll(def.Conditions, ctx)` — the full per-condition pass/fail list already exists per hand/deck card. Today `BuildEntry` only reduces it to one first-failing-condition string (`ActionCardEntry.UnplayableReason`), rendered by the `switch` in `CountryActionsView.BuildHandCard` (~lines 75-100, classes `action-card-unplayable-reason`). That switch/label is replaced by the new full-list rendering; `UnplayableReason` can remain in the data model for now (debug/back-compat) but the player-facing single-reason `Label` goes away.
  - Cost-affordability row reuses the same `canAffordGold` boolean `CountryActionsView.BuildHandCard` already computes (~line 62), rendered as one more requirement row instead of/alongside the existing separate cost footer styling — mirrors the debug menu's synthetic `gold >= N` row (`Assets/Scripts/Unity/UI/DebugCardAvailabilityView.cs`, `BuildExpandableCard`).
- **Wording layer (new — the debug label text is explicitly not reusable as-is):**
  - `ActionConditionDebugEntry.Label` (`src/Game.Configs/ActionConditionDebug.cs`, `Format`/`FormatOperand`) renders raw expression text like `"opinion (35) >= 30"` — the issue is explicit that player-facing text must not be technical. This spec needs a new mapping from condition `ExpressionNode.Type` (`opinion`, `control`, `totalCountryControl`, `targetRulerOrMilitaryOpinion`, `relationStillExists`, `hasSuitableRelationTarget`, `neitherSideAtWar`, `warFree`, `revengeEligible`, etc. — see `FormatOperand`'s switch for the full type list) plus its threshold to a short localized phrase, in the same spirit as the existing `action.country.unplayable.*` locale keys but per-condition instead of collapsed to one. See Ambiguities for exact phrasing per type and whether existing `action.country.unplayable.*` keys are reusable per-condition or new keys are needed.
- **Debug menu (`DebugCardAvailabilityView.cs`) is unaffected** — it remains the developer-only raw-condition view; the new player wording is an entirely separate presentation layer over the same `Conditions` data, not a replacement for the debug view.

### Playable-countries badge

- Mirror the war-win-chance badge's placement pattern exactly, but on the opposite side and with different chrome:
  - `.action-card-war-win-chance` (`OrgActions.uss`, ~lines 98-124): `position: absolute; top: 4px; right: 6px; ...` circular percent chrome, added inside `action-card-art` by `ActionCardBuilder.BuildWarWinChanceBadge` (lines ~165-176) when `warWinChancePercent.HasValue`, called from `Populate()` right after `artImage` is added (lines ~61-63).
  - New sibling element, e.g. `.action-card-playable-countries`, `position: absolute; top: 4px; left: 6px;` (mirrored side) — do **not** reuse the circular/percent styling; this badge holds flag chips, not a number. Added into the same `action-card-art` container by a new `ActionCardBuilder` builder method, sibling to the existing `if (warWinChancePercent.HasValue) {...}` block in `Populate()`.
- Flag chips: reuse the `.relations-flag` sizing/rendering convention (`CountryInfoView.cs` `BuildRelationsRow`, `~28-32px` square background image from `CountryVisualConfig.Find(countryId).flag`) rather than the larger `.entity-flag`. Up to 2 chips, `margin-left` on the non-first chip only (no `gap` support per `.claude/rules/unity/uitoolkit.md`), then a literal `"..."` `Label` appended when the underlying list has more than 2 entries (per the issue's exact wording — not a `"+N"` count).
- Tooltip: `TooltipSystem.RegisterTrigger(badgeElement, $"playable-countries-{card.ActionId}-{card.TargetCountryId}", ctx => BuildCountryListTooltip(fullList), new HashSet<string>())` — one tooltip on the whole badge element (not per-flag) listing every country in the full set, reusing `.relations-flag`-sized icons plus `country_name.{id}` locale keys, same construction pattern as `CountryInfoView.BuildRelationTooltip`.
- Data dependency on the sibling spec: "which countries could play this card" is a cross-country set that does not exist in today's model (a hand card today is already baked to one `TargetCountryId` / one country's deck). The sibling deck-rework spec (`Docs/Specs/26_08_06_10_card-deck-rework/spec.md`) is expected to define how that set is computed/exposed. This spec only defines how the badge renders once that list is supplied as data — the exact field name/shape on `ActionCardEntry` (or wherever it lands) is the sibling spec's concern, not this one's.
- Threading: `CountryActionsView.BuildHandCard`, `CardTransitionView.ShowCountry`, and `CardPlayAnimator.PopulateCountryTestCard` all need the new list threaded through as an additional optional parameter, following the same pattern `warWinChancePercent` already established end-to-end in `Docs/Specs/26_08_03_13_war-result-preview/spec.md`.
- Org cards: see Ambiguities — `OrgActionsView` cards are not per-country today, so whether this badge applies to them at all needs an explicit decision.

### Discard

- **Interaction — new pointer plumbing, following the project's mandated `PointerUpEvent` + manual `ContainsPoint` pattern (never `Button.clicked`/`ClickEvent`, per `.claude/rules/unity/uitoolkit.md`):**
  - Today `CountryActionsView.BuildHandCard` (~line 103) and `OrgActionsView.BuildHandCard` (~line 108) each register only one `PointerUpEvent` handler that plays the card on a quick tap. Add a `PointerDownEvent` handler on the same `cardEl` that starts a 1-second hold timer via `cardEl.schedule.Execute(...).StartingIn(1000)` — the same `IVisualElementScheduler`-based deferred-execution primitive `TooltipSystem`'s auto-pin (`AutoPinSeconds`) already uses, not a raw `Update()`-driven timer.
  - Track whether the pointer is still over the card via `PointerMoveEvent`/`PointerLeaveEvent` + `ContainsPoint`, so the release handler knows at release time whether to treat it as "still on card" or "moved off".
  - Coexistence with the existing play-on-release handler (both fire on `PointerUpEvent` on the same element): if the hold reached 1s and the hint is showing, release-on-card triggers discard instead of play; release-off-card cancels silently (matches the existing `ContainsPoint` guard already used for play). If the hold never reached 1s, release behaves exactly as today (play, unchanged). The discard branch must be checked and must short-circuit before the existing play branch to avoid double-firing.
- **Hint UI:** new small `VisualElement`/`Label` shown on/near the card once the hold timer fires, text `"Discard for {cost}"` followed by the existing gold-icon element — reuse the `.action-card-cost-icon` class already defined in `OrgActions.uss` / built by `ActionCardBuilder.cs` (the `costIcon` `VisualElement`) rather than adding a new icon asset. Hidden via `DisplayStyle.None`/`Flex` toggling, not `opacity`, since it should not intercept pointer events while hidden and should appear/disappear discretely.
- **Unaffordable price styling:** apply the existing `.action-card-cost-label--unaffordable` class (`OrgActions.uss`, `color: rgb(224, 64, 64)`) to the hint's price text when current gold `< DiscardGoldCost` — the exact same class `CountryActionsView.BuildHandCard`/`OrgActionsView.BuildHandCard` already apply to the normal cost footer label when unaffordable (`result.CostLabel.AddToClassList("action-card-cost-label--unaffordable")`).
- **Fly text on failed release:** inject `IFlyTextNotifier` (`Assets/Scripts/Unity/UI/IFlyTextNotifier.cs`, impl `FlyTextNotifierDocument`, already registered in `GameLifetimeScope`) into whichever class ends up owning the discard gesture; call `_flyText.Notify("action.discard.no_gold")` (new key) on release-with-insufficient-gold, mirroring existing call sites like `GameMenuDocument`'s `_flyText?.Notify("game_menu.save.confirmation")`. New locale key needs entries in both `Assets/Localization/en.asset` and a real Russian translation in `ru.asset` via the `localization` skill (not an English placeholder), per `.claude/rules/unity/localization.md`.
- **Configurable discard cost:** add `public double DiscardGoldCost { get; set; } = 50;` to `GameSettings` (`src/Game.Configs/GameSettings.cs`, alongside the existing `CardCooldownDays` — same flat-POCO tunable-value pattern). Mirror `"discardGoldCost": 50` into **both** `Assets/Configs/game_settings.json` and `src/Game.WebClient/wwwroot/configs/game_settings.json` — confirmed both files currently carry `"cardCooldownDays": 7` in sync at the same line (23), so the same dual-file update applies here; verify at implementation time whether one is generated from the other or both are hand-maintained. `HUDDocument.Construct` already injects `GameSettings gameSettings` — no new DI wiring is needed to read the value from wherever the discard gesture lives.
- **Gold deduction + discard + redraw:** implement as its own explicit small resource-mutation step (new command/system), not routed through the normal `ActionDefinition.Cost` play-cost pipeline (`src/Game.Systems/DeductActionCostSystem.cs`) since discarding is not playing a card — mirror that system's pattern (data-driven gold deduction) for consistency without depending on it directly. Reuse the same `gold >= N` affordability idiom already used by `src/Game.Systems/ActionPlayability.CanAfford`. Exact "redraw" semantics (does the discarded card's slot return to the deck, or is a different card drawn to refill the hand?) depend on the sibling deck-rework spec's draw mechanics — not defined here; see Ambiguities.
- **In-flight play animation edge case:** `CardPlayAnimator.IsPlaying` already gates other UI (`SuppressRefresh`) during an active play sequence. A new discard hold should not be startable while `IsPlaying` is true, and an in-progress hold/hint should be cancelled if `IsPlaying` becomes true mid-hold — named as a concrete edge case per the task brief, exact cancellation trigger left to Ambiguities/plan.
- **Org vs. country cards:** the issue's item 5 text does not distinguish card type; default assumption is both `CountryActionsView` and `OrgActionsView` hand cards get the discard gesture, applied independently in each view following the same pattern — flagged in Ambiguities since nothing in the issue confirms this explicitly.

## Out of Scope

- Issue items 0, 2, 4 (deck architecture / single deck per org / target-country context, action effect and naming rework, drawing cards without checking requirements) — covered by the sibling spec `Docs/Specs/26_08_06_10_card-deck-rework/spec.md`. This spec assumes that work lands separately and describes UI behavior (e.g. "requirements for the currently selected country/context") in terms that stay correct regardless of exactly how the sibling spec restructures targeting/decks underneath.
- Any change to how playability or draw eligibility is actually *computed* (`ActionPlayability`, `DrawCardSystem`, condition/config authoring) — this spec only surfaces already-computed `Conditions` data to players and adds the discard interaction; underlying gating logic and rules are unchanged.
- Character advisor cards (`Assets/Scripts/Unity/UI/CharactersView.cs`) — not an action card; no requirements list, playable-countries badge, or discard gesture there.
- The war-win-chance badge's own behavior, formula, or position — unchanged; this spec only mirrors its positioning pattern for the new left-side playable-countries badge.
- Redesign of card art, header, or description typography/content beyond what's needed to accommodate the taller card and the new requirements block.
- Sound or haptic feedback for the discard hold/hint/release gesture.
- `DebugCardAvailabilityView.cs` (developer debug menu) — remains as-is; the new player-facing requirement wording is a separate presentation layer over the same underlying condition data, not a replacement for the debug view.
- Any change to `GameSettings.CardCooldownDays` / cooldown UI — the discard feature is a new, independent card interaction, not a change to the existing cooldown overlay.

## Ambiguities

- [NEEDS CLARIFICATION: exact player-facing phrasing per condition type. Proposed scheme: `opinion >= N` → "Needs {N}+ opinion" (optionally naming the target role, mirroring today's `insufficient_opinion` reason which already includes the role name); `control >= N` / `totalCountryControl >= N` → "Needs {N}+ control"; `targetRulerOrMilitaryOpinion >= N` → "Target needs {N}+ opinion"; `relationStillExists` / `hasSuitableRelationTarget` → "Needs an active relation with a suitable country"; `neitherSideAtWar` / `warFree` / `not_at_war` / `already_at_war` → war-state-specific phrases; `revengeEligible` → "A country that wronged you must be eligible for revenge". Confirm exact copy per type, and whether existing `action.country.unplayable.*` keys can be reused per-condition or new keys are needed for a per-condition scheme.]
- [NEEDS CLARIFICATION: do org action cards (`OrgActionsView`, not country-targeted) get the playable-countries badge at all? They aren't drawn per-country today, so "countries which could play that card" may not cleanly apply — recommend no badge on org cards, but this needs an explicit yes/no.]
- [NEEDS CLARIFICATION: what does the requirements block show on org action cards, given org actions currently have empty `Conditions` and no per-condition playability data is wired for them (per `Docs/Specs/26_07_30_12_debug-card-availability/spec.md`'s own open ambiguity on this exact point)? Recommend: org cards show only the cost-affordability line as their sole requirement row until/unless real org conditions exist, but this is a product decision, not purely technical.]
- [NEEDS CLARIFICATION: exact +20% height math and layout placement — is the target literally `300 × 1.2 = 360px` total with header (32px) and art (130px) left unchanged and the entire +60px given to a requirements block appended inside the existing `action-card-body` below the current cost footer? Or should the requirements block be a new sibling section after `action-card-body` inside `action-card-content`? Also: if a card has 4+ requirement lines, does the block scroll/truncate, or does the description area shrink to make room? Needs a concrete answer before layout implementation.]
- [NEEDS CLARIFICATION: does the discard interaction apply to org action cards as well as country action cards, or is it country-cards-only like items 1 and 3 effectively are? The issue's item 5 wording doesn't scope it explicitly — this spec defaults to "both apply" but flags it for confirmation.]
- [NEEDS CLARIFICATION: when a discard hold/hint is in progress and a card-play animation (`CardPlayAnimator.IsPlaying`) starts elsewhere, should the in-progress hold be cancelled automatically, or should new holds simply be prevented from starting while `IsPlaying` is true (leaving an already-showing hint alone)? Recommend blocking new holds during `IsPlaying` and cancelling any in-progress hold if `IsPlaying` becomes true, mirroring the existing `SuppressRefresh`/`IsPlaying` guard pattern `CardPlayAnimator` already uses elsewhere — needs confirmation.]
- [NEEDS CLARIFICATION: does "discarded and re-drawn" mean a *different* card is drawn from the remaining deck to refill the hand slot (hand size preserved, discarded card instance is simply gone), or could the same discarded card come back later via the normal deck-return mechanics? This spec assumes the former (a fresh draw fills the vacated slot) since that's the most natural reading of the issue text, but the exact deck-return/redraw mechanics belong to the sibling deck-rework spec's territory and should be confirmed there or by the owner.]
