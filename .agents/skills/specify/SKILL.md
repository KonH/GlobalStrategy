---
name: specify
description: Capture feature intent for GlobalStrategy, including synchronization and issue-automation labeling, before planning.
---

# Specify

Follow `.claude/commands/specify.md` for GlobalStrategy synchronization, stage labels, paths, and overrides. When it reaches its Claude delegate, invoke `cd:specify` instead of `cc:specify`.
