---
name: dotnet-test
description: Run and diagnose GlobalStrategy's .NET tests with the shared captured-output workflow.
---

# .NET Test

Invoke `cd:dotnet-test` with `src/GlobalStrategy.Core.sln` as the default solution and `Debug` as the default configuration. Forward any user-supplied solution, project, configuration, filter, or test flags unchanged.
