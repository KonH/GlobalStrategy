---
name: plan
description: Create a GlobalStrategy implementation plan with its constitution, synchronization, labels, and Unity-specific user-step rules.
---

# Plan

Follow `.claude/commands/plan.md` for GlobalStrategy synchronization, stage labels, `Docs/Specs/` and `Docs/Constitution.md` paths, and the Unity User Steps override. When it reaches its Claude delegate, invoke `cd:plan` instead of `cc:plan`.
